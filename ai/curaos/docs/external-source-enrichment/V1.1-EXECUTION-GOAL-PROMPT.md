# v1.1 Execution Goal Prompt (XSRC + full v1.1 closure)

Paste the GOAL PROMPT below into a fresh Claude Code session at the workspace root to execute the full plan. Run with the suggested model + effort. It self-tracks progress in the local tracker + GitHub and stops only at the v1.1 predicate.

---

## Suggested model + effort

| Role | Model | Effort | Why |
|---|---|---|---|
| **Orchestrator (the session you paste this into)** | **Opus 4.8** | **ultracode (xhigh + workflow orchestration)** | Holds the wave plan, dependency graph, collision partition; spawns + verifies workers; makes the generator-first barrier calls. The hard reasoning is here. |
| Worker - generator/SDK/contract foundations (Wave 0) | Opus 4.8 | high | Mold changes ripple to every consumer; correctness-critical. Generator-first barrier work. |
| Worker - service/app implementation (Waves 1+) | Sonnet 4.6 | high | Bulk TDD implementation against settled contracts; Sonnet is the cost/throughput tier and clears the PHI minimum. |
| Worker - non-PHI mechanical (migrations, docs, fixtures, simple UI) | Sonnet 4.6 (or Haiku 4.5 only for non-PHI trivia) | medium | Cheap mechanical lanes. NEVER Haiku on any HealthStack/PHI path. |
| Reviewer - adversarial grill / PR gate (Tier-2) | Opus 4.8 (cross-harness Codex gpt-5.5 for opposite-harness grill) | high | Independent verification before merge per verification-stack rule. |

Concurrency: this machine = 18 cores -> **16 parallel lanes** (min(16, cores-2)). Per-harness tiering only (no cross-harness auto unless the grill step explicitly calls Codex).

---

## GOAL PROMPT (paste this)

```
GOAL: Execute the full CuraOS v1.1 working set to completion. Make every app + service 1:1 better than each mined open-source source AND better than all of them stacked, person-centric with zero feature loss, generator-first. Leave NO open v1.1 issue, blocker, unblock task, or near-term foresight item. Only far-future foresight that is genuinely out of v1.1 scope may be filed forward to v1.2.

AUTHORITATIVE INPUTS (read first, in order):
1. ai/curaos/docs/external-source-enrichment/README.md (program index)
2. ai/curaos/docs/external-source-enrichment/SUMMARY.md + FINAL-REPORT.md + PERSON-CENTRIC-LENS.md + DECISION-nextjs-vs-tanstack.md
3. ai/curaos/docs/external-source-enrichment/plan/implementation-backlog.json (178 items) + the exec-plan: ai/curaos/docs/external-source-enrichment/EXECUTION-PLAN.md (8-wave structure + lane lists + critical path)
3b. ai/curaos/docs/external-source-enrichment/identity/README.md + CUTOVER-PLAN.md (own multi-tenant IdM replacing pocket-id; ADRs 0237-0242; GH #862-868)
4. .ai-analysis/exec-plan/local-open-issues.json + the local tracker (.scratch/state/symphony-work/local-issues.sqlite, workflow_name=external-source-enrichment, target_phase=v1.1)
5. ADRs ai/curaos/docs/adr/0221-0236 (proposed - confirm/accept as you implement each)
6. The binding rules: generator-evolution, version-planning, local-vs-3rdparty, reuse-dry, verify-before-build, no-silent-block, person-centric lens.

EXECUTION DOCTRINE (non-negotiable):
- IDENTITY/OWN-IdM is in scope: build our own multi-tenant CIAM/IdP + user-lifecycle + B2B org-mgmt + entitlements (/oidc-provider via node-oidc-provider+jose, ReBAC, federation), grow identity-service + hosted-login, then operator-gated pocket-id cutover (stand-up -> migrate -> switch oidc-broker -> decommission, ADR-0242). IdP = highest-value attack surface; security-weighted.
- WAVE 0 FIRST: the generator/SDK/contract foundations (@curaos/x12, fhir-client, connector-sdk, tax-engine, contract-sdk, insurance-sdk, scheduling-sdk + codegen dual-surface/archetype emitter + @curaos/runtime-config). These are the upstream blockers. Run them max-parallel across DISTINCT packages; serialize anything touching curaos/tools/codegen. Do NOT dispatch downstream service workers while any codegen/SDK/contract lane is in-flight (the in-flight generator/SDK barrier).
- THEN service/app waves: each wave = a set of NON-COLLIDING lanes. Collision key = owner_path (submodule). Same submodule = serialize; different submodules = parallel. Pack each wave to 16 concurrent lanes.
- GENERATOR-FIRST always: the contract/SDK/codegen change lands BEFORE the consuming service/app; fold every edge case back into the generator, never hot-fix the generated artifact.
- PERSON-CENTRIC + NO FEATURE LOSS: every capability ships a person-facing surface AND a management surface over one contract; re-center source org-centric UX on the person; lose no business/compliance capability; prefer the simpler flow that still satisfies all of it.
- LICENSES: respect the license-risk-register verdicts. Permissive = vendor with NOTICE; GPL/AGPL = port-adapt clean-room or service-boundary, never copied into our build; adopt the Phase-14 tools where verdict=adopt (next-runtime-env-as-pattern -> build @curaos/runtime-config; node-x12 vendored+pinned; @medplum/fhir-router; cql-execution+@gorules/zen-engine; etc.).
- QUALITY: TDD red-green-refactor; unit + integration (real deps) + contract + E2E; demo/runtime data is DB-backed seeds (no runtime mocks); Tier-2 adversarial grill + PR gate before merge; verify RUNNING behavior before any build/deploy.

TRACKING (mandatory, every task):
- Every task/subtask/blocker/follow-up/verification gets a row in the local tracker BEFORE it becomes chat-only work (scripts/local-issues.js). Find the parent XSRC epic first; child via --parent-id.
- Mark in_progress on start, done on verified completion. Record evidence (commands + results) on each.
- Sync to the GitHub Project per the version-planning gate: the curated foundation issues already exist (your-org/curaos-ai-workspace#849-861); create child issues in the owning repo as each lane starts, wire as sub-issues, label ready-for-agent only when deps are satisfied.
- Branch per lane (never commit straight to main for code); PR -> Tier-2 grill -> merge; bump submodule pointer in curaos/ after each submodule lane lands.

PARALLELISM: use the Workflow tool to fan out each wave's non-colliding lanes concurrently (cap 16). One lane = one owner_path. Verify (status + diff + tests) after every worker report - trust but verify. If a synth/merge step crashes on large context, recover deterministically from the workflow journal.

STOP PREDICATE (when to stop): v1.1 is done ONLY when every v1.1 issue is acceptance-complete (not just closed), no open close-blocker carries v1.1, the product is a coherent working whole (builds + deploys per its models + GA E2E green), operator-gated live steps are executed-or-re-targeted, and the only remaining items are far-future v1.2 foresight. Do not stop while reachable v1.1 work or clearable blockers remain. Escalate any genuine blocker (credential/approval/decision/live-infra) same-turn with the exact unblock ask; never park work silently.

Begin by reading the inputs, reconciling the backlog + exec-plan + tracker into the current wave state, then dispatch Wave 0.
```

---

## How progress is tracked once running

- **Local tracker** is the source of truth: `node scripts/local-issues.js list --status open` shows remaining; each lane flips open -> in_progress -> done with evidence rows. v1.1 closure = zero open v1.1 rows except deferred-to-v1.2.
- **GitHub**: epic #849 + per-lane child issues mirror the tracker (ready-for-agent -> in PR -> closed); the org Roadmap Project aggregates cross-repo.
- **Session closeout** regenerates HANDOVER + ISSUE-ROADMAP from the tracker, so any session resumes mid-wave.
- **Definition of v1.1 done**: the stop predicate above; report it explicitly when reached.
```
