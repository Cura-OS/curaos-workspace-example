## Verdict: REJECT

## P0 findings (if any)

- P0 | Q1/Q2/Q4 | src/identity-core/divergence/divergence-ledger.store.ts:375 | Two rolling-overlap checker instances can erase a committed divergence and advance the durable offset past it. `PostgresDivergenceLedgerStore.persist()` replaces the whole ledger from one instance's local snapshot by deleting every row at `src/identity-core/divergence/divergence-ledger.store.ts:375-380`, then re-inserting only that local snapshot's counters/pending rows at `src/identity-core/divergence/divergence-ledger.store.ts:381-419`. The only DB uniqueness guard is the partial counter index at `src/identity-core/db/schema.ts:431-439`; it prevents duplicate counter rows, but it does not serialize writers, merge stale snapshots, protect the offset row, or compare a loaded version. Concrete false-green: instance A records a divergent pair through `recordDurable()` and persists a counter at `src/identity-core/divergence/audit-divergence-checker.ts:508-515`; instance B, started from an older clean snapshot, later persists clean counters/pending plus a newer offset, deleting A's rows and writing only B's offset at `src/identity-core/divergence/divergence-ledger.store.ts:410-419`. A restart then rehydrates the clean snapshot at `src/identity-core/divergence/audit-divergence-checker.ts:489-496`; after warm-up, `isGreen()` returns true when divergence and pending counts are zero at `src/identity-core/divergence/audit-divergence-checker.ts:396-400`. Fix: make ledger updates concurrency-safe with a single-writer/advisory lock or serializable CAS/version row that covers load+merge+persist+offset, and persist offsets only with the merged state that includes every committed divergence.

## P1 findings

- P1 | Q3 | src/identity-core/divergence/divergence-ledger.store.ts:126 | `assertReferenceOnly()` is real runtime code, but its value whitelist is too broad for a PHI boundary. It checks each `change.values[]` only with `REFERENCE_VALUE_RE = /^[A-Za-z0-9._:#@-]+$/` at `src/identity-core/divergence/divergence-ledger.store.ts:126-129` and rejects only values that fail that regex at `src/identity-core/divergence/divergence-ledger.store.ts:173-180`. That admits identifier-shaped PHI/credentials such as `alice@example.com`, `1980-01-01`, or `123-45-6789`; the stricter Diamond audit schema already shows the expected closed reference model and PHI scan at `src/identity-core/audit/audit-event.schema.ts:85-97` and `src/identity-core/audit/audit-event.schema.ts:154-188`. Fix: replace the open regex with the same closed reference schema/PHI scan used by `AuditEventEnvelopeSchema`, including max lengths and closed value domains.

- P1 | Q3 | src/identity-core/divergence/divergence-ledger.store.ts:157 | `assertReferenceOnly()` never validates `fact.correlationId`, yet Postgres persists it both as the `correlation_id` column and inside `pending_fact`. The pending-fact loop validates `tenantId`, `actorId`, `operationType`, and `changes[].values` at `src/identity-core/divergence/divergence-ledger.store.ts:157-181`, but does not inspect `correlationId`; `NormalizedAuditFact` leaves `correlationId` as an unrestricted string at `src/identity-core/divergence/normalized-audit-fact.ts:123-125`; the M3 and Diamond normalizers copy caller/event correlation IDs directly at `src/identity-core/divergence/audit-normalizers.ts:114-126` and `src/identity-core/divergence/audit-normalizers.ts:182-198`; and the store writes `fact.correlationId` into `correlation_id` plus the full JSON fact at `src/identity-core/divergence/divergence-ledger.store.ts:395-405`. `operation_type` is protected by the closed enum checks at `src/identity-core/divergence/divergence-ledger.store.ts:146-150` and `src/identity-core/divergence/divergence-ledger.store.ts:168-172`; `correlation_id` is not. Fix: validate `correlationId` as an opaque reference only, preferably UUID/closed reference token, before persist and reject PHI-shaped values.

## P2 findings

- P2 | Q1 | src/identity-core/divergence/audit-divergence-checker.ts:418 | `markReplayComplete()` is an unconditional public setter, so the checker cannot prove the replay actually reached stream head before green is allowed. A durable checker starts fail-closed when a ledger is wired at `src/identity-core/divergence/audit-divergence-checker.ts:251-260`, and `isGreen()` correctly returns false while `replayComplete` is false at `src/identity-core/divergence/audit-divergence-checker.ts:396-400`; but `markReplayComplete()` simply flips that bit at `src/identity-core/divergence/audit-divergence-checker.ts:418-420` with no head offset, partition watermark, or ledger verification. The restart test demonstrates an empty durable checker becomes green after this call at `test/integration/divergence/audit-divergence-restart.test.ts:134-151`. Fix: require `markReplayComplete()` to receive/verify the consumed high-water mark against the durable offset or move the call behind a replay service that owns the stream-head proof.

- P2 | Q2 | src/identity-core/divergence/divergence-ledger.store.ts:422 | `PostgresDivergenceLedgerStore` only makes snapshot replacement atomic when the executor exposes `transaction`; otherwise it runs `DELETE` plus all inserts without rollback. The executor interface makes `transaction` optional at `src/identity-core/divergence/divergence-ledger.store.ts:263-267`, and `persist()` falls back to `await run(this.db)` at `src/identity-core/divergence/divergence-ledger.store.ts:422-426`. A mid-write failure in that fallback can leave a torn ledger even though the offset/state contract depends on one atomic snapshot. Fix: require a transactional executor for Postgres, fail closed if `transaction` is absent, or implement explicit transaction SQL before the delete/insert sequence.

## P3 findings

- P3 | Q6 | test/integration/divergence/audit-divergence-restart.test.ts:72 | The restart test does create a new checker instance and does not share checker memory, but it only shares an in-memory ledger object; it does not prove file/Postgres durability across a process restart. The test persists via `const ledger = new InMemoryDivergenceLedgerStore()` at `test/integration/divergence/audit-divergence-restart.test.ts:72`, constructs a new checker at `test/integration/divergence/audit-divergence-restart.test.ts:93-94`, and the in-memory store clones snapshots on load/persist at `src/identity-core/divergence/divergence-ledger.store.ts:212-218`, so it is not merely reusing the old checker state. However, the Postgres test explicitly says real Postgres integration is out of scope at `test/identity-core/divergence/divergence-ledger.postgres.test.ts:13-15`. Fix: add a restart test that uses a fresh `FileDivergenceLedgerStore` or real Postgres-backed store object after the first checker is discarded.

- No P3 finding | Q5 | drizzle/migrations/0002_divergence_ledger_add.sql:47 | Migration shape is forward-only and re-runnable: it uses `CREATE TABLE IF NOT EXISTS` at `drizzle/migrations/0002_divergence_ledger_add.sql:47-60` and `CREATE INDEX IF NOT EXISTS` at `drizzle/migrations/0002_divergence_ledger_add.sql:65-71`, and the schema comment states it is an in-place add with no `-v2` path at `src/identity-core/db/schema.ts:410-411`. I found no DROP/ALTER or parallel replacement path in the migration.

## Re-grill verification (2026-05-30, post-4a6c830) — cycle 1

**Verdict: REJECT** (Codex cross-harness, HIGH-BLAST auth). Counters now safe; pending-erase + PHI single-token holes survive.

| Prior finding | Status | Evidence |
|---|---|---|
| P0 lost-update (counter) | **FIXED** | text-GREATEST numeric bug AVOIDED — cast to bigint before GREATEST (store.ts:601-604); counters monotonic. |
| P0 lost-update (pending) | **STILL-BROKEN** | store.ts:569-584 + 638-648 + audit-divergence-checker.ts:508-515: an authoritative writer with an EMPTY local pending set still executes `DELETE pending WHERE TRUE` → erases another pod's real pending divergence never merged into this writer's snapshot → false-green survives. |
| P1 PHI whitelist | **STILL-BROKEN** | store.ts:181-193/216-220/239-240: `isOpaqueReferenceToken()` only rejects email/DOB/SSN/whitespace; a single-token raw name `membership:JaneDoe#clinician` / `membership:Jane-Doe#clinician` (no whitespace) passes. |
| P1 correlationId bypass | FIXED | now routed through guard. |

**New (lower sev):** global advisory lock (all tenants/ops) = bottleneck not blocker (store.ts:563-566); InMemory/File store paths still full-replace (no merge, no fail-closed) (store.ts:342-344/369-372).

**Text-GREATEST numeric-comparison question: NO bug** — bigint cast prevents lexicographic compare.

**Required before merge (cycle 2):** (1) close the pending-erase P0 — a stale/empty-snapshot writer must NEVER delete pending it didn't author; scope the prune to only the pending rows this writer actually resolved (by id/correlation), never `WHERE TRUE`. (2) PHI grammar must reject single-token raw names — UUID must match full UUID shape, role must match the closed RBAC enum exactly, `membership:<uuid>#<role>` target must be a UUID (not a free name). (3) Real-Postgres integration test (the fake regex executor can't prove advisory-lock/ON CONFLICT/cast semantics) — worker's own FORESIGHT flagged this.

## Re-grill verification (2026-05-30, post-8d48242) — cycle 2

**Reviewer:** Codex (adversarial harness) | **HEAD:** 8d48242 | **Branch:** agent/m9-202-durable-divergence-claude-0f25daf8

---

### P0 pending-erase — STILL-BROKEN

**What changed (good):** The old `DELETE … WHERE TRUE` path is fixed for the empty-resolved case. `recordDurable()` in `audit-divergence-checker.ts:545–560` builds `resolvedCorrelationIds`, excludes ids still in local pending, and passes the exact set to `persist()`. `divergence-ledger.store.ts:770–778` guards the Postgres DELETE behind `authoritative && resolvedIds.length > 0` — an authoritative writer with empty resolved set never issues a DELETE. Same-path re-occupy is also safe: a displaced same-path fact is counted unresolved and the newest pending entry stays (`audit-divergence-checker.ts:287–305`); still-pending ids are excluded from the prune set (`:551–559`).

**Why it is still P0:** Pending rows are keyed by bare `correlation_id`, not `(tenant_id, correlation_id)`.

- Migration unique index: `CREATE UNIQUE INDEX … ON divergence_ledger_pending (correlation_id)` — `drizzle/migrations/0002_divergence_ledger_add.sql:79–81`. No tenant partition.
- Runtime upsert conflict target: `ON CONFLICT (correlation_id) DO UPDATE` — `divergence-ledger.store.ts:735–756`.
- Runtime DELETE predicate: `WHERE kind='pending' AND correlation_id IN (…)` — `divergence-ledger.store.ts:770–776`. No tenant filter.
- InMemory/File store keyed by bare `correlationId` — `divergence-ledger.store.ts:392–407`.

Per grill protocol: absence of tenant partition key on pending rows = P0. A resolved `correlation_id` from tenant A is structurally identical to a legitimately-pending `correlation_id` from tenant B if any cross-tenant ID reuse occurs (UUIDs are per-writer, not guaranteed globally unique across tenants in a multi-tenant schema). The DELETE will erase tenant B's row.

---

### P1 PHI grammar — STILL-BROKEN (narrow vector survives)

**What changed (good):** `membership:JaneDoe#clinician` is now rejected. The `<target>` field of a membership token requires an anchored UUID regex and `<role>` requires a closed `RBAC_ROLES` enum match (`divergence-ledger.store.ts:121–122`, `:164–166`, `:240–245`; `rbac-types.ts:1–8`). Tests cover raw-name target, `#superuser`, wrong-length UUID, and valid UUID+enum (`divergence-ledger.postgres.test.ts:521–587`).

**Surviving bypass:** The `correlationId` field accepts `isOpaqueReferenceToken()` (`divergence-ledger.store.ts:263–265`, `:311–321`), which rejects only whitespace/email/DOB/SSN patterns before accepting the broad `REFERENCE_TOKEN_RE` (`:168–175`, `:182–188`, `:202–215`). A `correlationId` of `"JaneDoe"` passes. That value is persisted as both the `correlation_id` column and inside the `pending_fact` JSONB blob (`:735–748`). Tightest bypass: valid `membership:<uuid>#clinician` in `changes[].values`, `correlationId: "JaneDoe"` — the membership validator fires on the value, clears it; the raw bare name lands in the correlation column and blob unchallenged.

---

### Regression from resolved-tracking — CONFIRM-FIXED

Counter monotonicity: pair/unresolved increments + `Math.max()` / Postgres `GREATEST(…::bigint)` remain intact (`audit-divergence-checker.ts:336–379`; `divergence-ledger.store.ts:377–389`, `:711–729`). Offset-before-state ordering: resolved set is built from already-persisted state before the persist call (`audit-divergence-checker.ts:545–560`; `divergence-ledger.store.ts:711–815`). Fail-closed cold-start / `awaitingReplay` guard survives (`audit-divergence-checker.ts:264–272`, `:433–437`; `identity-core.module.ts:122–137`). InMemory/File stores were made merge-not-replace; restart tests pass.

No regression introduced by resolved-tracking threading. This item is clean.

---

### Real-PG test integrity — NEW-ISSUE (partial)

Skip guard is clean: `test/integration/divergence/divergence-ledger.real-postgres.test.ts:43–44` — skips conditionally on missing `CURAOS_IDENTITY_DATABASE_URL`, does not silently always-pass. Targeted run: 32 pass, 5 skip (real-PG), 0 fail.

When DSN is present the test uses real postgres.js transactions (`:78–99`) and exercises the advisory-lock/ON CONFLICT/`::bigint` cast paths through runtime SQL — paths the fake store cannot model. This is genuine progress.

**Gaps:**
- DDL in the test mirrors the bare pending unique index (`:110–140`) — so the cross-tenant overwrite scenario is not modelled.
- Concurrency coverage is counter-only (`:180–201`).
- Pending-survival assertion is sequential single-tenant A-then-B (`:204–231`). The cycle-1 false-green (authoritative writer with empty local pending running against another pod's live pending rows) is not proved impossible by this test.

The test is better than nothing but does not constitute proof of the pending-erase fix for the cross-tenant/cross-pod scenario.

---

### Verdict: REJECT

**Pending-erase definitively closed: NO**

Two blockers remain before merge:

1. **(P0) Tenant partition key missing on pending rows.** Add `tenant_id` to the pending unique index and to every DELETE/upsert WHERE clause in both migration and runtime store (all three store implementations). Until then the scoped-delete fix is structurally incomplete: correct on bare correlation_id reuse within one tenant, unsafe across tenants.

2. **(P1) `correlationId` accepts bare names.** Extend `isOpaqueReferenceToken()` or add a separate validator that rejects tokens matching a human-name pattern (letters-only, no UUID structure, no `Type:<uuid>` form). Alternatively, validate `correlationId` against the same UUID regex required for membership targets, since all first-party correlation IDs are UUIDs.

The regression analysis is clean. Real-PG test structure is sound; it needs the tenant-keyed scenario added once P0 is fixed.

## Re-grill verification (2026-05-30, post-922b6f9) — cycle 3 FINAL

### Verdict: REJECT

### P0 Cross-tenant (cycle-2 vector) — STILL-BROKEN
For each sub-check (a)–(e): status + file:line evidence or MISSING.

(a) STILL-BROKEN — Postgres inserts `tenant_id` for pending rows at src/identity-core/divergence/divergence-ledger.store.ts:741 and src/identity-core/divergence/divergence-ledger.store.ts:744, but the pending upsert target is still bare `ON CONFLICT (correlation_id) WHERE kind = 'pending'` at src/identity-core/divergence/divergence-ledger.store.ts:750, and the DELETE still prunes only by `kind` + `correlation_id IN (...)` at src/identity-core/divergence/divergence-ledger.store.ts:774 and src/identity-core/divergence/divergence-ledger.store.ts:775.

(b) STILL-BROKEN — InMemory persists through `mergeSnapshots(...)` at src/identity-core/divergence/divergence-ledger.store.ts:441, but that helper keys current pending by bare `f.correlationId` at src/identity-core/divergence/divergence-ledger.store.ts:396, keys incoming pending by bare `f.correlationId` at src/identity-core/divergence/divergence-ledger.store.ts:399, and deletes by bare `cid` at src/identity-core/divergence/divergence-ledger.store.ts:405.

(c) STILL-BROKEN — File store delegates to the same `mergeSnapshots(current, snapshot, ...)` path at src/identity-core/divergence/divergence-ledger.store.ts:480, so it inherits the bare pending keys at src/identity-core/divergence/divergence-ledger.store.ts:396 and src/identity-core/divergence/divergence-ledger.store.ts:399 and the bare delete at src/identity-core/divergence/divergence-ledger.store.ts:405.

(d) STILL-BROKEN — MISSING claimed drop-bare-recreate-composite migration. The 0002 migration still creates `divergence_ledger_pending_unique` on bare `(correlation_id)` at drizzle/migrations/0002_divergence_ledger_add.sql:79 and drizzle/migrations/0002_divergence_ledger_add.sql:80, scoped only by `WHERE kind = 'pending'` at drizzle/migrations/0002_divergence_ledger_add.sql:81. No `DROP INDEX` line exists in this migration; safe concurrent-writer/idempotent drop handling is therefore MISSING. The file is forward-only/no split by comment at drizzle/migrations/0002_divergence_ledger_add.sql:73 and drizzle/migrations/0002_divergence_ledger_add.sql:74, but it is still the wrong bare-index shape.

(e) STILL-BROKEN — `tenant_id` is nullable in schema.ts because `tenantId: uuid('tenant_id')` has no `.notNull()` at src/identity-core/db/schema.ts:418, and it is nullable in the SQL migration because `tenant_id uuid` has no `NOT NULL` at drizzle/migrations/0002_divergence_ledger_add.sql:50. The only table check is the kind check at src/identity-core/db/schema.ts:451, src/identity-core/db/schema.ts:452, and src/identity-core/db/schema.ts:453 / drizzle/migrations/0002_divergence_ledger_add.sql:58 and drizzle/migrations/0002_divergence_ledger_add.sql:59. App persist rejects a pending fact without UUID tenantId at src/identity-core/divergence/divergence-ledger.store.ts:296 and src/identity-core/divergence/divergence-ledger.store.ts:297, but DB shape does not enforce it; a NULL `tenant_id` row would not match a future `tenant_id = $t` equality prune and could silently remain leaked.

### P1 correlationId UUID validation (cycle-2 vector) — STILL-BROKEN
For each sub-check (a)–(c): status + file:line evidence or MISSING.

(a) STILL-BROKEN — The UUID literal regex is defined at src/identity-core/divergence/divergence-ledger.store.ts:121 and src/identity-core/divergence/divergence-ledger.store.ts:122, but correlationId validation does not require that regex: `isReferenceOnlyCorrelationId()` returns `isOpaqueReferenceToken(value)` at src/identity-core/divergence/divergence-ledger.store.ts:263 and src/identity-core/divergence/divergence-ledger.store.ts:264. `isOpaqueReferenceToken()` accepts non-PHI broad ASCII tokens through `REFERENCE_TOKEN_RE` at src/identity-core/divergence/divergence-ledger.store.ts:202 and returns that result at src/identity-core/divergence/divergence-ledger.store.ts:214; `looksLikePhi()` only checks whitespace/email/DOB/SSN at src/identity-core/divergence/divergence-ledger.store.ts:182, src/identity-core/divergence/divergence-ledger.store.ts:183, src/identity-core/divergence/divergence-ledger.store.ts:184, src/identity-core/divergence/divergence-ledger.store.ts:185, src/identity-core/divergence/divergence-ledger.store.ts:186, and src/identity-core/divergence/divergence-ledger.store.ts:187. A bare `JaneDoe` still fits the broad token regex, so UUID-only validation is MISSING.

(b) STILL-BROKEN — Uppercase UUIDs are accepted by `[A-F]` in the UUID regex at src/identity-core/divergence/divergence-ledger.store.ts:122. Braced `{uuid}` is rejected because braces are not in `REFERENCE_TOKEN_RE` at src/identity-core/divergence/divergence-ledger.store.ts:202 and the UUID regex is anchored at src/identity-core/divergence/divergence-ledger.store.ts:121. `urn:uuid:<uuid>` is falsely accepted by the broad token regex because letters, colon, and hyphen are all allowed at src/identity-core/divergence/divergence-ledger.store.ts:202 and returned at src/identity-core/divergence/divergence-ledger.store.ts:214.

(c) PARTIAL — validation is called before Postgres persist at src/identity-core/divergence/divergence-ledger.store.ts:669, and the checked `fact.correlationId` is then written to the `correlation_id` column at src/identity-core/divergence/divergence-ledger.store.ts:746 and embedded in `pending_fact` via `JSON.stringify(fact)` at src/identity-core/divergence/divergence-ledger.store.ts:747. That means both storage paths are covered by one guard, but the guard is not UUID-only per src/identity-core/divergence/divergence-ledger.store.ts:263 and src/identity-core/divergence/divergence-ledger.store.ts:264.

### Cycle-2 clean vectors — regression check — NO REGRESSION
Empty-snapshot false-green closed — NO REGRESSION: durable checkers with a ledger default to `replayComplete = false` at src/identity-core/divergence/audit-divergence-checker.ts:268, src/identity-core/divergence/audit-divergence-checker.ts:269, src/identity-core/divergence/audit-divergence-checker.ts:270, src/identity-core/divergence/audit-divergence-checker.ts:271, and src/identity-core/divergence/audit-divergence-checker.ts:272; `isGreen()` returns false while replay is incomplete at src/identity-core/divergence/audit-divergence-checker.ts:433, src/identity-core/divergence/audit-divergence-checker.ts:434, and src/identity-core/divergence/audit-divergence-checker.ts:435.

Counter monotonic (bigint cast) — NO REGRESSION: in-memory/file merge uses `Math.max(...)` at src/identity-core/divergence/divergence-ledger.store.ts:388, and Postgres uses `GREATEST(...::bigint, EXCLUDED...::bigint)` at src/identity-core/divergence/divergence-ledger.store.ts:724, src/identity-core/divergence/divergence-ledger.store.ts:725, src/identity-core/divergence/divergence-ledger.store.ts:726, src/identity-core/divergence/divergence-ledger.store.ts:727, and src/identity-core/divergence/divergence-ledger.store.ts:728.

Same-path re-occupy safe — NO REGRESSION: same-path collision records the displaced fact as unresolved and keeps the newest fact pending at src/identity-core/divergence/audit-divergence-checker.ts:287, src/identity-core/divergence/audit-divergence-checker.ts:304, and src/identity-core/divergence/audit-divergence-checker.ts:305; durable prune excludes correlation IDs still pending at src/identity-core/divergence/audit-divergence-checker.ts:556, src/identity-core/divergence/audit-divergence-checker.ts:557, and src/identity-core/divergence/audit-divergence-checker.ts:558.

Offset-before-state ordering — NO REGRESSION: `recordDurable()` records the fact, updates `lastOffset`, then persists the snapshot before returning at src/identity-core/divergence/audit-divergence-checker.ts:545, src/identity-core/divergence/audit-divergence-checker.ts:546, src/identity-core/divergence/audit-divergence-checker.ts:547, src/identity-core/divergence/audit-divergence-checker.ts:548, src/identity-core/divergence/audit-divergence-checker.ts:559, and src/identity-core/divergence/audit-divergence-checker.ts:560. Postgres writes counters/pending before the offset block at src/identity-core/divergence/divergence-ledger.store.ts:711 and src/identity-core/divergence/divergence-ledger.store.ts:735, then advances offset at src/identity-core/divergence/divergence-ledger.store.ts:780 and src/identity-core/divergence/divergence-ledger.store.ts:783.

Fail-closed cold-start — NO REGRESSION: the module constructs the durable checker with `new PostgresDivergenceLedgerStore(db)` at src/identity-core/identity-core.module.ts:128 and src/identity-core/identity-core.module.ts:129, constructs `new AuthDiamondDivergenceChecker({ ledger })` at src/identity-core/identity-core.module.ts:130, rehydrates at src/identity-core/identity-core.module.ts:136, and returns without calling `markReplayComplete()` at src/identity-core/identity-core.module.ts:137; `isGreen()` fail-closes before replay complete at src/identity-core/divergence/audit-divergence-checker.ts:433, src/identity-core/divergence/audit-divergence-checker.ts:434, and src/identity-core/divergence/audit-divergence-checker.ts:435.

### Cross-tenant real-PG test — UNSOUND
Evidence from test file: tenant setup, shared correlation_id, assert B's pending survives, real-PG or mock.

Tenant setup is not cross-tenant: the file defines one `TENANT` constant at test/integration/divergence/divergence-ledger.real-postgres.test.ts:47, `pendingFact` uses that tenant at test/integration/divergence/divergence-ledger.real-postgres.test.ts:54, and the pending-survival test's writer B counter uses the same `TENANT` at test/integration/divergence/divergence-ledger.real-postgres.test.ts:219. Shared correlation_id across two tenants is MISSING: the only base pending correlation is `TARGET` at test/integration/divergence/divergence-ledger.real-postgres.test.ts:58, and no second tenant pending fact is created. The A-then-B sequence is sequential, not concurrent: A persists at test/integration/divergence/divergence-ledger.real-postgres.test.ts:213 and B persists at test/integration/divergence/divergence-ledger.real-postgres.test.ts:223. The assertion checks only that A's same-tenant pending correlation survives at test/integration/divergence/divergence-ledger.real-postgres.test.ts:225, test/integration/divergence/divergence-ledger.real-postgres.test.ts:226, and test/integration/divergence/divergence-ledger.real-postgres.test.ts:227. The test is REAL Postgres when `CURAOS_IDENTITY_DATABASE_URL` is present: it imports `postgres` at test/integration/divergence/divergence-ledger.real-postgres.test.ts:35, gates live tests on DSN at test/integration/divergence/divergence-ledger.real-postgres.test.ts:43 and test/integration/divergence/divergence-ledger.real-postgres.test.ts:44, creates a postgres.js client at test/integration/divergence/divergence-ledger.real-postgres.test.ts:106, and executes SQL through `client.unsafe(...)` at test/integration/divergence/divergence-ledger.real-postgres.test.ts:83 and test/integration/divergence/divergence-ledger.real-postgres.test.ts:84.

### Final merge-gate answer
**Safe to merge: NO**
REJECT: cycle-2 P0 remains structurally open because Postgres still upserts/deletes pending rows by bare `correlation_id` at src/identity-core/divergence/divergence-ledger.store.ts:750 and src/identity-core/divergence/divergence-ledger.store.ts:775, InMemory/File still inherit bare `correlationId` merge keys at src/identity-core/divergence/divergence-ledger.store.ts:396 and src/identity-core/divergence/divergence-ledger.store.ts:399, and migration/schema still expose the bare pending index / nullable tenant at drizzle/migrations/0002_divergence_ledger_add.sql:79 and src/identity-core/db/schema.ts:418. Cycle-2 P1 remains open because `correlationId` still delegates to broad opaque-token validation at src/identity-core/divergence/divergence-ledger.store.ts:263, src/identity-core/divergence/divergence-ledger.store.ts:264, src/identity-core/divergence/divergence-ledger.store.ts:202, and src/identity-core/divergence/divergence-ledger.store.ts:214.

## POST-MERGE REGRESSION NOTICE (2026-05-30) — orchestrator error

**PR #46 was merged at cycle-2 code (`95d243e` == `8d48242`), NOT cycle-3.** The cycle-3 fix commit `922b6f9` is NOT in identity-service main (`git merge-base --is-ancestor 922b6f9 origin/main` → false). The orchestrator merged on a cycle-3 re-grill that returned **REJECT** (the grill correctly reported the fixes absent at the checked-out worktree HEAD `8d48242`); the merge was an orchestrator mistake — it trusted the worker's cycle-3 *claim* without confirming the push landed.

**Two HIGH-blast-auth vectors are LIVE in identity-service main + bumped into curaos:**
1. **P0 cross-tenant pending-erase** — `deletePending(_tenantId, correlationIds)` (divergence-ledger.store.ts:772) ignores tenant (param is `_tenantId`), SQL = `DELETE … WHERE kind='pending' AND correlation_id IN (...)` with NO tenant filter; pending unique index (0002 migration:79-81) is on bare `correlation_id`. Tenant A's resolved id deletes tenant B's pending divergence → cross-tenant false-green on #99 Phase D gate.
2. **P1 correlationId not UUID-validated** — bare names accepted into correlation_id + pending_fact jsonb.

**Mitigating factors:** #99 Phase D is itself blocked-by-external (live staging signal, not yet active); no live consumer wired yet (worker FORESIGHT); the checker is not yet gating production. So the exposure is latent, not actively false-greening today. Still must fix before #99 Phase D goes live.

**Fix-forward plan (rolling-update rule — no history rewrite):** forward migration `0003` recreating the pending unique index as composite `(tenant_id, correlation_id) WHERE kind='pending'` + code: `deletePending` adds `AND tenant_id = $t`, ON CONFLICT pending target → composite, all 3 stores (Postgres/InMemory/File), correlationId UUID-validated. Real-PG cross-tenant test. New issue + grill before merge.

---

## FIX-FORWARD LANDED (2026-05-30) — PR identity-service#49 → id-svc main `d9992a7`

Issue #202 reopened (it was wrongly CLOSED on the fictional `#221` closeout while this grill stood at REJECT). The cross-tenant fix-forward was implemented on a fresh branch off id-svc `95d243e`, head-verified-grilled, and admin-squash-merged as `d9992a7` (PR #49). curaos pointer bumped `de099db`→`d9992a7`.

**What landed (commits `f50c98e` cross-tenant fix + `e64794c` migration-safety hardening):**
- Composite pending key `(tenant_id, correlation_id)` everywhere: Postgres `ON CONFLICT (tenant_id, correlation_id)` + tenant-scoped `DELETE … (tenant_id, correlation_id) IN ((t,c),…)`; InMemory/File `mergeSnapshots` via a shared `pendingKey()`; the checker threads `DivergenceResolvedRef {tenantId, correlationId}` pairs keyed by the composite `pairKey` (was a bare `Set<string>`).
- `correlationId` UUID-validated (`isReferenceOnlyCorrelationId` → `UUID_RE`): every first-party correlation id is a UUID (`user.id`/`session.id`/`targetUserId`); a bare name, dotted ref, or `urn:uuid:` prefix is rejected at the PHI boundary.
- `0003` forward migration (migration-safe): pre-clean unprunable NULL-tenant pending rows → CREATE composite index (`…_v2`) BEFORE DROP bare → rename into place (no no-uniqueness window) → `tenant_id NOT NULL` CHECK added `NOT VALID` then `VALIDATE` (no abort on populated tables). `migrations.ts` carries byte-equivalent DDL.

**Verification:** real-Postgres cross-tenant test (two tenants share a correlation_id; A resolves; **B's pending SURVIVES**) + NULL-tenant-rejected DB-check test — 7/7 real-PG, 85/85 divergence, 294/294 no-DB, typecheck clean. Live cycle-2→cycle-3 migration upgrade proven idempotent + safe (seeded a bare-index table with a NULL-tenant pending row; migration lifts to composite, pre-cleans, validates, rejects).

### Re-grill verification (2026-05-30, post-`e64794c`) — head-verified ACCEPT
**Reviewer:** Codex (opposite-harness) | **grilled_sha:** `e64794c` (== PR head) | **HIGH-BLAST auth**

- Round 1 (`f50c98e`): **all code blockers FIXED** — P0(a) Postgres composite ON CONFLICT/DELETE; (b) InMemory composite `mergeSnapshots`; (c) File store inherits; (d) 0003 composite index; (e) tenant-not-null CHECK; P1 UUID-only `correlationId`; checker tenant-scoped `DivergenceResolvedRef`. Cross-tenant test sound (two tenants, shared correlation_id, A resolves, B survives, composite bootstrap DDL). **REJECTed on 2 HIGH migration hazards**: index swap not rolling-deploy safe; tenant-not-null CHECK would abort on a populated cycle-2 table with NULL-tenant pending rows.
- Round 2 (`e64794c`): **ACCEPT** — both hazards CLOSED (create-before-drop-rename; pre-clean + NOT VALID/VALIDATE); migration idempotent; final index name correct; pre-clean DELETE scoped to bad rows only; cross-tenant code fix untouched by the hardening commit; no new hazards.

**Net status: cross-tenant pending-erase false-green CLOSED + migration rolling-deploy-safe.** Verified on the MERGED SHA (`d9992a7` contains `f50c98e`+`e64794c`).
