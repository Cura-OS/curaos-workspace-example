# Grill Report — M8-S3 Zarf package manifest (curaos#91 + workspace#138)

## Initial grill verdict (2026-05-27): APPROVE-WITH-CONDITIONS

### Findings (P0/P1/P2/P3 with file:line refs)

#### P0 (blocks merge)

None.

#### P1 (must fix before merge)

1. K3s kube-proxy-disable contract is inconsistent across the PR pair. The implementation uses `--disable-kube-proxy` in `curaos/ops/zarf/zarf.yaml:152`, while the module docs still say the required flag is `--disable=kube-proxy` in `ai/curaos/ops/zarf/AGENTS.md:45` and `curaos/ops/zarf/README.md:70-71`. Hypothesis: if the M8-S3 checklist is enforced literally, this fails the required Cilium-safe flag check; if `ai/rules/curaos_cni_rule.md:37-43` is treated as canonical, the implementation is probably right and the module docs/checklist wording are stale. Either way, merge should not leave the contract split.

2. Tenant variables are declared but not all used in component templates. `NAMESPACE`, `DOMAIN`, `ENABLE_HARBOR`, and `ENABLE_OBSERVABILITY` are declared in `curaos/ops/zarf/zarf.yaml:73-88`; only `NAMESPACE` is actually templated into components at `curaos/ops/zarf/zarf.yaml:311` and `curaos/ops/zarf/zarf.yaml:340`. `DOMAIN` appears only as prose in `curaos/ops/zarf/zarf.yaml:335`, and the optional toggles appear only in selector comments at `curaos/ops/zarf/zarf.yaml:105-116`. That fails checklist item 6 as written.

3. Cross-tool digest gate language reappears in docs even though the v4 contract forbids it. `ai/curaos/ops/zarf/CONTEXT.md:72-74` says a CI digest-parity gate fails on BuildKit ↔ Buildah divergence; `curaos/ops/zarf/component-budget.md:124-126` says the release pipeline fails if BuildKit and Buildah digests diverge; `ai/curaos/docs/adr/0164-zarf-bundle-layout.md:332-339` still names a digest-parity gate. The workflow and order script do not introduce BK ≡ BA comparison, but PR-B docs still point future M8-S4 work toward the forbidden gate.

#### P2 (file as followup issue)

1. Layer numbering is internally confusing even though the component count is correct. `ai/curaos/ops/zarf/CONTEXT.md:12` says 10 components are Layer 0-9; `ai/curaos/ops/zarf/CONTEXT.md:22` says `NAMESPACE` applies to Layer 8+9; `ai/curaos/ops/zarf/CONTEXT.md:95` says Layer 8 migration Jobs run before Layer 9 services; but the manifest labels `curaos-migration-jobs` as Layer 7 and `curaos-services` as Layer 8 at `curaos/ops/zarf/zarf.yaml:300-330`. This is not the 9-component drift from M8-S1, but it is still brittle doc drift.

#### P3 (nit)

None.

### Coverage checklist
- [x] declaration order
- [x] Helm hook weight
- [x] K3s install flags
- [x] optional component flags
- [x] digest pin discipline
- [x] tenant variables
- [x] schema validation
- [x] order-check script
- [x] CI workflow gates
- [x] DOC-GRAPH consistency
- [x] AGENTS.md/CONTEXT.md/Requirements.md 10-component count
- [x] no cross-tool digest gate
- [x] submodule pointer in PR-B
- [x] cross-PR closing refs

### Summary

Declaration order matches ADR-0164 §2.1 exactly: `zarf-registry-init`, `curaos-k3s-init`, `cilium-cni`, `cnpg-operator`, `redpanda`, `harbor-registry`, `glitchtip-pyrra`, `signing-trust`, `curaos-migration-jobs`, `curaos-services` at `curaos/ops/zarf/zarf.yaml:129-330`. `bash tools/build/zarf-deploy-order-check.sh` exits 0 and reports 10/10 components.

Helm hook weight is present as `"-5"` in the migration stub at `curaos/ops/zarf/manifests/migration-jobs.yaml:23-25`. Optional Zarf components are marked `required: false` for Harbor and observability at `curaos/ops/zarf/zarf.yaml:243-267`. All observed image refs under `curaos/ops/zarf` carry `@sha256:` real digests or `@sha256:<digest>` placeholders; no tag-only image refs were found.

Schema validation passed with `bunx ajv-cli@5.0.0 validate --spec=draft2020 -s /tmp/zarf-v076.schema.json -d ops/zarf/zarf.yaml --strict=false` (`ops/zarf/zarf.yaml valid`), and schema compile passed with the same draft2020 schema. `bun scripts/check-doc-graph.js` exits 0 (`doc graph ok (835 nodes, 5411 edges)`). PR-B submodule pointer bumps `curaos` from `f5c8986` to `e26a74ee997f3f687c21fab755d27f1ffe38f534`, exactly matching the local curaos PR-A branch head.

Live PR body verification could not run from this harness: `gh auth status` reports invalid `GITHUB_TOKEN`, and `env -u GITHUB_TOKEN gh pr view ...` cannot connect to `api.github.com`. Local branch head commits for both repos include `Closes your-org/curaos-ai-workspace#85`, but live PR bodies were not independently verified.
