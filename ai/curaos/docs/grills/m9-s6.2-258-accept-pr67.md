# Grill — M9-S6.2 invitation accept (`accepted.v1`) — PR identity-service#67

- **Issue:** your-org/curaos-ai-workspace#258 ([M9-S6.2], story #103)
- **PR:** your-org/identity-service#67
- **Branch:** `agent/feat-identity-invitation-accept-claude-c219f0b6`
- **Commit:** `9052039`
- **Reviewer harness:** Codex (`codex exec`, account-default model, reasoning effort high, `--sandbox read-only`)
- **Implementer harness:** Claude Code
- **Scope:** implementation PLAN grill (pre-final-implementation), per one-task-execution §4 Adversarial Grill Gate
- **Date:** 2026-06-01

This was a **plan-stage** grill (run after issue selection, before the final implementation, on a read-only sandbox). Findings were auto-applied into the implementation per `ai/rules/curaos_recommendation_auto_apply_rule.md` (each had a clear recommendation grounded in the codebase). No critical flag blocked the build; no user-escalation candidate surfaced (no irreversible/destructive/T3 decision, no unapproved scope).

## Verdict

Strong grill. Every P0/P1 resolved in-code; P2s either applied or documented as intentional replay semantics. The accepting-actor question (P0) was resolved by the issue scope (keep the #257 write-scope posture; the service enforces tenant isolation) rather than a re-prompt, since changing RBAC logic is explicitly out of scope for #258.

## Findings + resolutions

| # | Sev | Finding | Resolution |
|---|-----|---------|------------|
| 1 | P0 | Cross-tenant accept hole — repo only had `findById`, no tenant scope; leaked UUID lets tenant B accept/probe tenant A. | Added `findByTenantAndId` + tenant-scoped conditional `markAccepted (WHERE tenant_id=? AND id=? AND status='pending')`. Tenant check FIRST in the service → cross-tenant = 404 (no existence/status leak). Test: `cross-tenant accept is rejected`. |
| 3 | P0 | Accepting-actor semantics unresolved (invitee vs admin). | Resolved by issue scope: keep #257 write-scope posture (`RequiresInvitationScope write` = tenant-admin); the service enforces tenant match. Changing RBAC/auth model is out of scope for #258 (it touches access-control logic — a T3 surface). Documented in the controller header. |
| 5 | P0 | Trace continuity not rolling-safe — #257 rows have no `trace_id`; old pods insert NULL post-migration. | Forward-only `trace_id` column; invite persists `principal.traceId`; accept reads it back. Legacy NULL row → fall back to the accepting principal's request traceId (never blocks a legacy invite). Documented in the migration + schema + service. |
| 7 | P0 | Double-accept race unless `markAccepted` is a single conditional mutation. | `markAccepted` is one conditional UPDATE returning the row; loser sees `undefined`, re-reads in-tx, and no-ops if already accepted. Tests: `double-accept idempotent`, conditional guard in both repos. |
| 9 | P1 | Audit chain CAS must be in the SAME tx as status update + accepted.v1 enqueue. | All three run inside `outbox.transaction`; audit publish bound via `auditOutbox.bindTo(tx.db)` (the #257 durable-before-ack pattern). Test: `durable audit-outbox is enqueued on the production durable-before-ack path`. |
| 11 | P1 | CAS conflict behavior unspecified. | The status UPDATE is the primary guard, so a benign same-invitation audit race cannot occur on the happy path (the row already left `pending`); a genuine CAS conflict throws `ConflictException` and rolls back the tx (status + event + audit drop together). No silent partial commit. |
| 13 | P1 | Migration helper mismatch — `schema.ts`, `migrations.ts` helper, both repos all need the columns. | Updated all four: `schema.ts` (`traceId`/`expiresAt`), `addInvitationAcceptColumns()` helper (called in both `ensureIdentityCoreSchema` + `ensureTenantIdentityCoreSchema`), in-memory + Drizzle repo insert + new methods. |
| 15 | P1 | `expires_at` null/TTL semantics hidden. | NULLable, NULL = no expiry; boundary `expiresAt <= now` = expired; app clock (service threads `now`). Documented in migration + schema + service. Test: `accepting an expired invitation rejects cleanly`. |
| 17 | P1 | PHI leak via response shape — `InvitationRow` carries `inviteeEmail`. | Controller continues to use the `toResource` mapper (already excludes `inviteeEmail`/`traceId`/`expiresAt`); the raw row is never serialized. |
| 19 | P1 | Audit schema trap — `changeValues.status: ['accepted']` fails `AuditEventEnvelopeSchema` (`'accepted'` is not RBAC role / UUID / allowlisted ref). | The status VALUE is omitted from `changeValues`; the transition is named in `changedFields: ['status']`; `changeValues` carries only the opaque `org_id` UUID. Verified against `audit-event.schema.ts` closed-domain `ChangeReferenceValueSchema`. Test: `audit envelope is reference-only`. |
| 21 | P2 | Divergence pairing regression surface (don't collapse `correlationId` into `traceId`; don't touch admin role-grant). | correlationId stays per-leg on both legs; the admin path is untouched. The existing `admin-controller-diamond-audit.test.ts` already asserts `correlationId=targetUserId`, so the regression guard is in place. Test: `correlationId stays per-leg, NOT collapsed into traceId`. |
| 23 | P2 | Accepted-event idempotency key unspecified. | `Accepted:<tenantId>:<invitationId>` (stable, tenant-namespaced) — a second guard behind the status transition. |
| 25 | P2 | Conflict response ordering (404 vs 403 vs 409). | Explicit: unknown/cross-tenant → 404 (tenant check first, no leak); revoked/expired → 409; idempotent already-accepted → 200 (no-op). |
| 27 | P2 | Duplicate-accept audit trade-off — no-op suppresses repeated-attempt audit visibility. | Intentional replay semantics (documented in the service `accept` docstring) — matches the invite leg's idempotency-replay behavior. |

## Evidence

- `bun run ci` GREEN: lint (0 errors), typecheck, `459 pass / 40 skip / 0 fail` across 499 tests, test:scripts PASS, build (`tsc`).
- New tests: `invitations-accept.service.test.ts` (15), `accepted-event.contract.test.ts` (6), `invitations-accept-migration.test.ts` (3), `invitations.http.test.ts` (+1 accept-route).

> **Note:** this is the plan-stage grill record. The mandatory T2 PR gate (opposite-harness grill of the landed diff — new endpoint + new topic + audit emission) is the orchestrator's responsibility before merge per `ai/rules/curaos_verification_stack_rule.md`. A re-grill of the diff appends a `## Re-grill verification` section to this file.
