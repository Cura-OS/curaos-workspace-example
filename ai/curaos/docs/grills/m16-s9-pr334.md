# Adversarial Grill: M16-S9 PR #334 (CNPG Postgres-per-service Cluster CRs + DSN wiring)

- Target: PR your-org/curaos#334 (M16-S9 #687)
- Branch: agent/feat-ops-cnpg-clusters-claude-8d3ba1ea1e
- Reviewer: Codex (opposite-harness, read-only)
- Date: 2026-06-13
- Scope: PHI database isolation, tenant data boundary, air-gap, DSN wiring

## VERDICT: FAIL

Highest severity: P0. The repo's PHI-boundary clinical inventory includes `scheduling-service` and `terminology-service`, but this PR wires both to the neutral `curaos-data` Citus coordinator path.

## Service-to-Cluster Mapping

| Service | Observed mapping | Verdict |
|---|---|---|
| encounter-service | PHI inventory; no neutral DB CR or neutral values entry | OK if #684 has PHI source |
| scheduling-service | `values.zarf.yaml:128` and `cnpg-clusters.yaml:898-904` -> `curaos-data` / `curaos-citus-coordinator` | FAIL |
| clinical-doc-service | PHI inventory; no neutral DB CR or neutral values entry | OK if #684 has PHI source |
| orders-service | PHI inventory; no neutral DB CR or neutral values entry | OK if #684 has PHI source |
| terminology-service | `values.zarf.yaml:134` and `cnpg-clusters.yaml:970-976` -> `curaos-data` / `curaos-citus-coordinator` | FAIL |
| business-patient-service, patient-core-service, personal-patient-service, healthstack-* services listed at `cnpg-clusters.yaml:54-70` | Expected `healthstack-pg`; no neutral DB CR observed, but no machine-readable PHI service map in `values.zarf.yaml` | Needs explicit PHI map |

## Findings

| Severity | file:line | break | fix |
|---|---|---|---|
| P0 | `backend/packages/healthstack-phi-boundary/src/clinical-services.ts:9-15`, `ops/zarf/charts/curaos-umbrella/values.zarf.yaml:128`, `ops/zarf/manifests/cnpg-clusters.yaml:898-904` | `scheduling-service` is a clinical PHI service but is provisioned as neutral. Quote: `'scheduling-service'`; `scheduling-service: scheduling`; `name: scheduling-service-db`, `namespace: curaos-data`, `name: curaos-citus-coordinator`. | Remove it from neutral DB wiring or first change the PHI-boundary inventory. Add it to a machine-readable PHI map consumed by #684/provisioning. |
| P0 | `backend/packages/healthstack-phi-boundary/src/clinical-services.ts:14`, `backend/packages/healthstack-phi-boundary/src/clinical-services.ts:71-76`, `ops/zarf/charts/curaos-umbrella/values.zarf.yaml:134`, `ops/zarf/manifests/cnpg-clusters.yaml:970-976` | `terminology-service` is clinical PHI with active schema, but is provisioned as neutral. Quote: `'terminology-service'`; `status: 'active'`; `terminology-service: terminology`; `name: terminology-service-db`, `namespace: curaos-data`, `name: curaos-citus-coordinator`. | Remove it from neutral DB wiring and route it through `healthstack-pg`, or change the PHI inventory first. |
| P1 | `ops/zarf/charts/curaos-umbrella/values.zarf.yaml:40-47`, `ops/zarf/charts/curaos-umbrella/values.zarf.yaml:60-64`, `ops/zarf/manifests/cnpg-clusters.yaml:166-172` | Neutral DB-per-service isolation is not proven. The config uses one cluster app secret and every neutral DB uses `owner: app`. Hypothesis, based on standard Postgres role behavior: same role/password plus changed dbname lets one service connect to other service DBs. | Use per-service DB owners/roles and per-service secrets, or prove live grants prevent cross-service reads. |
| P1 | `ops/migrations/job-template.yaml:79-83`, `ops/migrations/job-template.yaml:109-113`, `ops/zarf/charts/curaos-umbrella/values.zarf.yaml:53-54` | Runtime and migrator read `<service>-db-app` key `uri`, but this PR only defines a pattern and metadata; no Secret or ExternalSecret emission was found. | Add secret emission now, or gate service deployment until #684 creates every required secret. |
| P1 | `ops/zarf/manifests/cnpg-citus-cluster.yaml:18`, `ops/zarf/manifests/cnpg-citus-cluster.yaml:82`, `ops/zarf/zarf.yaml:253-265` | Strict digest packaging still fails because the shipped old Citus manifest contains `@sha256:<digest>`. Command: `bash tools/build/zarf-digest-check.sh --strict` exited 1 with `FAIL: One or more M8-S1 placeholders remain; --strict refuses zarf package create.` | Backfill both old Citus `imageName` fields with the real digest already used at `zarf.yaml:238` and `cnpg-clusters.yaml:97`. |
| P2 | `ops/zarf/cnpg-clusters.test.ts:283-303` | The focused test passes because it only checks neutral CR/map consistency, so wrong PHI-to-neutral classification is self-consistent. `bun test ops/zarf/cnpg-clusters.test.ts` passed 19 tests. | Assert every `CLINICAL_SERVICES` entry is absent from `postgres.neutral.databases` and neutral Database CRs. |
| P2 | `ops/zarf/charts/curaos-umbrella/values.zarf.yaml:139-143` | `postgres.phi` has cluster metadata only, not a service list, despite this file claiming to be the canonical service-to-cluster map. | Add `postgres.phi.services` or equivalent. |
| P3 | `ops/zarf/cnpg-clusters.test.ts:110`, `ops/zarf/cnpg-clusters.test.ts:168`, `ops/zarf/cnpg-clusters.test.ts:236`, `ops/zarf/cnpg-clusters.test.ts:324`, `ops/zarf/zarf.yaml:251` | Agent provenance comments like `Codex grill finding` remain. No AI co-author trailer found in PR commits. No en dash or em dash found in scanned diff/files. | Remove provenance labels from comments. |

## Other checks

- Duplicate cluster check: no duplicate Citus cluster or name clash found. The new file declares only `healthstack-pg`; old Citus coordinator/workers remain separate.
- Air-gap check: ordering is correct (storage, old Citus, then new clusters in `zarf.yaml:246-265`); PHI PVs exist at `airgap-local-path-storage.yaml:101-148`.
- Hygiene: no en/em dash in scanned diff/files; no AI co-author trailer in PR commits.

## Gate decision

FAIL. Two P0 PHI-boundary violations (scheduling-service, terminology-service routed to neutral curaos-data) plus P1 unproven per-service DB isolation and an un-backfilled strict-digest placeholder block merge.
