# Opposite Harness Grill Blocked

GRILL: blocked-harness-unavailable
GRILL-PROBE: {"available":false,"reason":"opposite-harness grill report missing","evidence":"{\"verdict\":\"pass\",\"issues\":[],\"report_path\":\"\"}"}
GRILL-HARNESS: codex
GRILL-AGENT: codex:codex-rescue
GRILL-TIMEOUT-MS: 420000
GRILL-REASON: grill-result-report-path-missing-or-mismatched

The opposite-harness adversarial leg failed fast and no CodeRabbit-only fallback should be treated as a completed opposite-harness grill.
Subject: M16-S2 (curaos-ai-workspace#538): umbrella-chart dependencies aggregation. Plan: add tools/codegen/src/umbrella-emit.ts that (1) discovers the 87 service slugs by parsing .gitmodules backend/services/<slug> entries (canonical registry per 2026-06-07 research; no registry file exists), sorted deterministically, (2) emits ops/zarf/charts/curaos-umbrella/Chart.yaml with real version 0.1.0 + appVersion 0.1.0 (matching the per-service chart Chart.yaml.hbs floor) + a dependencies block: one entry per slug {name: <slug>, version: 0.1.0, repository: file://../../../../backend/services/<slug>/chart}, replacing the 0.1.0-stub version + the STUB/M8-S3 sentinel header. Wire an umbrella subcommand into tools/codegen/src/index.ts (dry-run default, --write to emit). Update tools/build/zarf-digest-check.sh guard #4 to PASS when chart has a real (non-stub) version + a non-empty dependencies block, and STILL FAIL on stub reversion (0.1.0-stub or M8-S3 sentinel or empty/commented deps). Keep guards 1/2/3/5 intact. Tests: a fixture integration test that scaffolds the audit core trio to a tmp dir via emitServiceLive, emits per-service charts + the umbrella into that same tmp layout (umbrella deps pointing at the tmp service charts), then runs helm dependency build + helm template on the umbrella green with graceful-skip when helm absent (S1 pattern); plus a unit test asserting the deps block content + no em-dash + stub sentinel absent + guard #4 pass/fail behavior. Note: S3 (regen of real 87) has NOT run, so the COMMITTED real Chart.yaml references subcharts whose files arrive in S3; the committed-tree helm dependency build is documented as S3-verified, while the emitter is proven green against the fixture. Constraints: generator-owned (deps emitted, never hand-authored), no em-dashes/en-dashes anywhere, do not touch the 6 pre-existing codegen failures (curaos#263), one accountable author no AI trailers.
