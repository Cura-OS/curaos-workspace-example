# M16-S3 batch Helm-chart emitter (curaos PR #265) grill

Subject: your-org/curaos-ai-workspace#539 / curaos PR #265.

## Opposite-harness grill (Claude -> Codex)

GRILL: completed-with-conditions
GRILL-HARNESS: codex
GRILL-MODEL: gpt-5.5 (gpt-5.1-codex-max rejected on ChatGPT account: "model is not supported")
GRILL-OUTPUT: read-only review; returned a conforming 7-section verdict.

### Codex verdict (verbatim sections, condensed)

**1. Missing questions**: all answered by current state: 87 submodules present + writable; `gen:charts-all --write` could create a `chart/` for an absent submodule dir (mitigated: every dir is present + the bulk driver now rebases on `origin/main`); direct push to each service `main` was the approved verified-batch path (handoff-authorized).

**2. docs/ADR conflicts (VALID, FIXED)**: `ops/zarf/zarf.yaml:452,461`, `ops/zarf/README.md:25,320`, `ops/zarf/component-budget.md:176-177`, `ops/zarf/values/values-demo.yaml:13-15` still called the umbrella a stub "wired in M8-S3". Fixed in commit 879e0dd: updated to "real (M16-S2 emitted deps; M16-S3 charted all 87 services); zarf component wiring lands in M16-S4 (#540)". No ADR conflict.

**3. glossary conflicts**: none.

**4. hidden deps/subtasks (VALID, FIXED)**: the regen driver logged emit/push errors but did not fail-fast or recover non-fast-forward pushes. Fixed in 879e0dd: per service it now `git fetch origin main` + rebases onto `origin/main` before commit (the ff fix), clears a stale `index.lock`, disables the global `hooksPath` for the commit/push (the fsmonitor ENOTDIR fix), and accumulates failures into a final non-zero exit.

**5. prototype candidates**: covered by the test suite + the live run (one service per layer + overlay; stale-main + emitter-throw + absent-dir paths; strict 87-dependency parse).

**6. decision points with recommended answers (auto-applied)**: codex independently verified the slug set: "core 27, plain 36, business 12, personal 12; no suspicious mappings" and "strict parse resolves 87/87". Recommendations (keep mapping precedence, keep plain->service-core, keep `file://` depth + version 0.1.0, make the driver fail-fast) all match the shipped design / were applied.

**7. user-escalation candidates**: codex flagged the live `scripts/charts-all-regen.sh` (commit+push across 87 service mains + 87 pointer bumps) as needing explicit approval. This was the explicit, handoff-authorized task ("chart + commit + push a clear batch") for an additive, generator-emitted, helm-lint-proven change; executed as a verified batch with full per-service status logging.

## Implementer self-audit (corroborates the grill)

- slug -> layer: all 87 charts have `name == slug` (0 mismatches); codex independently confirmed the 27/36/12/12 split.
- trio symmetry: 7 pure-Helm template files byte-identical (md5) across service-{core,personal,business} chart trees.
- chart-only emission: a seeded `src/audit.service.ts` survives a re-emit (test-locked); each of the 87 service commits is chart-only.
- idempotency: a second run writes 0 files (test-locked).
- umbrella: `helm dependency build` resolves all 87 `file://` refs ("Saving 87 charts", exit 0); `helm template` renders 87 Deployments/Services/ConfigMaps/ServiceAccounts (exit 0).

## Verdict

T2 opposite-harness leg COMPLETED. Two valid findings (stale Zarf docs; regen-driver robustness) both fixed in commit 879e0dd. Codex independently corroborated the slug mapping + 87/87 umbrella resolution. No blocking finding. The change is additive, generator-owned, idempotent, and proven end-to-end with real helm v4.2.0.
