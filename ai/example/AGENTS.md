# Example Project Agent Contract

Read this before changing files under the example project.

## Scope

This folder represents the project-specific agent documentation mirror. It intentionally contains only a small sanitized sample.

## Rules

- Keep product code outside `ai/`.
- Keep project context in `CONTEXT.md`.
- Link to shared rules instead of copying them.
- Add module-level docs only after the matching module exists in the code tree.
