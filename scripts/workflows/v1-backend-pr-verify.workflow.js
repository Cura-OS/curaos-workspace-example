export const meta = {
  name: 'v1-backend-pr-verify',
  description: 'T2 verify pass over the v1 backend build-wave PRs (epic #734): per PR a read-only 3-lens review (Correctness/Security/Contract) scaled by blast radius - PHI/RBAC/codegen PRs get a deep adversarial lens that tries to BREAK the PHI-isolation / RBAC / generator-mold invariant. Returns a structured merge verdict per PR (merge | fix-first | hold) with findings. Read-only: the orchestrator merges serially in dependency order from these verdicts.',
  phases: [
    { title: 'Verify', detail: 'per PR: blast-scaled review, adversarial for PHI/RBAC/codegen (parallel, read-only)' },
  ],
}

const VERDICT = { type: 'object', required: ['ref', 'verdict'], properties: {
  ref: { type: 'string' },
  verdict: { type: 'string', enum: ['merge', 'fix-first', 'hold'] },
  blast: { type: 'string' },
  findings: { type: 'array', items: { type: 'object', required: ['sev', 'what'], properties: {
    sev: { type: 'string', enum: ['P0', 'P1', 'P2', 'P3'] }, what: { type: 'string' }, file: { type: 'string' } } } },
  summary: { type: 'string' },
  emDashOrTrailer: { type: 'boolean', description: 'true if any em/en dash or AI-attribution commit trailer found (a blocker)' } } }

const DEFAULT_PRS = [{"num": 812, "ref": "curaos-ai-workspace#812", "repo": "your-org/curaos-ai-workspace", "title": "docs(tracking): v1 cross-core overlay contract mirror + pointer (#746)", "blast": "docs"}, {"num": 811, "ref": "curaos-ai-workspace#811", "repo": "your-org/curaos-ai-workspace", "title": "docs(shop): v1 shop overlay mirror + --core-base generator fold-back (#743, #776)", "blast": "docs"}, {"num": 392, "ref": "curaos#392", "repo": "your-org/curaos", "title": "feat(codegen): --overlay-core for cross-core personal/business overlays", "blast": "codegen"}, {"num": 391, "ref": "curaos#391", "repo": "your-org/curaos", "title": "feat(codegen): cross-root overlay --core-base flag + shop overlay pointers (#743, #776)", "blast": "codegen"}, {"num": 9, "ref": "personal-tracking-service#9", "repo": "your-org/personal-tracking-service", "title": "feat(tracking): v1 contract - location feed, geofence CRUD, SOS (cross-core overlay)", "blast": "service"}, {"num": 1, "ref": "personal-shop-service#1", "repo": "your-org/personal-shop-service", "title": "feat(personal-shop): v1 individual storefront overlay over commerce-core (#776)", "blast": "service"}, {"num": 50, "ref": "business-shop-service#50", "repo": "your-org/business-shop-service", "title": "feat(business-shop): v1 org storefront overlay over commerce-core (#743)", "blast": "service"}, {"num": 810, "ref": "curaos-ai-workspace#810", "repo": "your-org/curaos-ai-workspace", "title": "docs(automation): v1 contract-mock status in ai/curaos mirror (#739/#741/#745)", "blast": "docs"}, {"num": 1, "ref": "business-automation-service#1", "repo": "your-org/business-automation-service", "title": "feat(business-automation): editor CRUD + governance + monitoring (v1) (#741)", "blast": "service"}, {"num": 23, "ref": "personal-automation-service#23", "repo": "your-org/personal-automation-service", "title": "feat(personal-automation): node-editor CRUD + run-history + trigger (v1) (#745)", "blast": "service"}, {"num": 1, "ref": "automation-core-service#1", "repo": "your-org/automation-core-service", "title": "feat(automation-core): connector catalogue + automation contract (v1) (#739)", "blast": "service"}, {"num": 809, "ref": "curaos-ai-workspace#809", "repo": "your-org/curaos-ai-workspace", "title": "docs(donation): refresh ai mirror for v1 business + personal overlays (#742, #774)", "blast": "docs"}, {"num": 390, "ref": "curaos#390", "repo": "your-org/curaos", "title": "chore(workflow): bump personal + business workflow-service pointers to v1 overlays (#744 #747)", "blast": "pointer"}, {"num": 1, "ref": "personal-donation-service#1", "repo": "your-org/personal-donation-service", "title": "feat(personal-donation): build v1 contract overlay over donation-core (#774)", "blast": "service"}, {"num": 28, "ref": "business-workflow-service#28", "repo": "your-org/business-workflow-service", "title": "feat(business-workflow): v1 overlay + process-definition governance contract (#744)", "blast": "service"}, {"num": 17, "ref": "business-donation-service#17", "repo": "your-org/business-donation-service", "title": "feat(business-donation): build v1 contract overlay over donation-core (#742)", "blast": "service"}, {"num": 1, "ref": "personal-workflow-service#1", "repo": "your-org/personal-workflow-service", "title": "feat(personal-workflow): v1 overlay + workflow CRUD + trigger contract (#747)", "blast": "service"}, {"num": 1, "ref": "personal-site-service#1", "repo": "your-org/personal-site-service", "title": "feat(personal-site): v1 site-builder overlay (CRUD, publish, canvas, template gallery)", "blast": "service"}, {"num": 37, "ref": "business-site-service#37", "repo": "your-org/business-site-service", "title": "feat(business-site): v1 site-builder overlay (CRUD, publish, canvas, commerce embed)", "blast": "service"}, {"num": 9, "ref": "personal-calendar-service#9", "repo": "your-org/personal-calendar-service", "title": "feat(personal-calendar): v1 booking-link contract over calendar-core (#748)", "blast": "service"}, {"num": 389, "ref": "curaos#389", "repo": "your-org/curaos", "title": "feat(healthstack-billing): scaffold service + author patient billing contract (#737)", "blast": "phi"}, {"num": 808, "ref": "curaos-ai-workspace#808", "repo": "your-org/curaos-ai-workspace", "title": "feat(healthstack-messaging): mirror docs + curaos pointer for secure-messaging service (#738)", "blast": "phi"}, {"num": 388, "ref": "curaos#388", "repo": "your-org/curaos", "title": "feat(healthstack-messaging): secure-messaging overlay service (#738)", "blast": "phi"}, {"num": 807, "ref": "curaos-ai-workspace#807", "repo": "your-org/curaos-ai-workspace", "title": "feat(personal-notes): v1 notes contract - mirror docs + pointer (#775)", "blast": "docs"}, {"num": 806, "ref": "curaos-ai-workspace#806", "repo": "your-org/curaos-ai-workspace", "title": "docs(tenancy-core-service): ai/curaos mirror for neutral tenant lifecycle (#735)", "blast": "rbac"}, {"num": 387, "ref": "curaos#387", "repo": "your-org/curaos", "title": "feat(tenancy-core-service): neutral tenant lifecycle service to v1 contract parity (#735)", "blast": "rbac"}, {"num": 386, "ref": "curaos#386", "repo": "your-org/curaos", "title": "chore(submodules): bump personal-notes-service to v1 notes contract (#775)", "blast": "pointer"}, {"num": 385, "ref": "curaos#385", "repo": "your-org/curaos", "title": "feat(plugin-runtime): neutral plugin control plane (catalogue + lifecycle + seams)", "blast": "service"}, {"num": 1, "ref": "personal-notes-service#1", "repo": "your-org/personal-notes-service", "title": "feat(personal-notes): scaffold service + v1 notes contract (#775)", "blast": "service"}, {"num": 384, "ref": "curaos#384", "repo": "your-org/curaos", "title": "feat(healthstack-consent): bump pointer to v1 consent contract", "blast": "phi"}, {"num": 1, "ref": "healthstack-consent-service#1", "repo": "your-org/healthstack-consent-service", "title": "feat(healthstack-consent): v1 consent capture contract + service scaffold", "blast": "phi"}]

function prPrompt(pr) {
  const deep = (pr.blast === 'phi' || pr.blast === 'rbac' || pr.blast === 'codegen')
  const lens = pr.blast === 'phi'
    ? 'DEEP ADVERSARIAL (PHI): try to BREAK the PHI/PII-isolation invariant - PHI must live ONLY in the overlay schema, neutral services carry references + metadata only. Find any PHI field leaking into a neutral service, an unguarded PHI read, a missing consent gate, or PHI in an event payload/log.'
    : pr.blast === 'rbac'
    ? 'DEEP ADVERSARIAL (RBAC/tenant-isolation): try to BREAK tenant isolation + RBAC - find any cross-tenant data path, a tenant id taken from the request body instead of the auth context, a missing RBAC guard on a mutation, or a tenant CRUD op that does not scope by the caller tenant.'
    : pr.blast === 'codegen'
    ? 'DEEP ADVERSARIAL (generator mold, FLEET-WIDE blast): this changes tools/codegen - a defect propagates to every future service. Try to BREAK it: does the new flag (--core-base / --overlay-core) emit a correct lock + Dockerfile COPY set for the cross-core overlay? Does it regress the existing trio/overlay paths? Are there tests? Does re-emit stay idempotent? Is the #739 fail-closed lock guard still honored?'
    : 'three read-only lenses: Correctness (logic/edge/error-handling), Security (input/authz/secrets), Contract (the .tsp REST resources + AsyncAPI events match the issue spec; controller in lock-step with the .tsp; SDK-generatable).'
  return 'Read-only T2 review of PR ' + pr.ref + ' (repo ' + pr.repo + ', #' + pr.num + '): "' + pr.title + '". Blast=' + pr.blast + '.\n\n' +
    'Inspect the diff via: env -u GITHUB_TOKEN gh pr diff ' + pr.num + ' --repo ' + pr.repo + ' (and gh pr view for the body). Do NOT modify or merge anything.\n\n' +
    'Apply ' + lens + '\n\n' +
    'ALSO check (blocker if present): any em/en dash character in the diff/commits, OR any AI-attribution commit trailer (Co-authored-by / Generated-by / etc.) on the branch commits (git log on the PR branch). Set emDashOrTrailer=true if found.\n\n' +
    'For a v1 backend service PR the BAR is contract-level: a real .tsp with the spec REST resources + a controller exposing them + a replayable mock + the AsyncAPI events + ai/curaos mirror docs (mirror docs may be in a SEPARATE curaos-ai-workspace PR - do not fail this PR for that). Full persistence is a plus, not required.\n\n' +
    'Return: ref="' + pr.ref + '", blast="' + pr.blast + '", verdict (merge = no P0/P1 + bar met; fix-first = P0/P1 fixable; hold = needs a decision/dependency), findings (sev P0-P3 + what + file), summary, emDashOrTrailer.' +
    (deep ? ' Default verdict=fix-first if you find ANY plausible P0/P1 on the adversarial invariant; only return merge when you actively verified the invariant holds.' : '')
}

phase('Verify')
const verdicts = await parallel(DEFAULT_PRS.map((pr) => () =>
  agent(prPrompt(pr), { label: 'verify:' + pr.ref, phase: 'Verify', model: 'opus', schema: VERDICT })
    .catch((e) => ({ ref: pr.ref, blast: pr.blast, verdict: 'hold', summary: 'review errored: ' + String((e && e.message) || e), findings: [] }))
)).then((r) => r.filter(Boolean))

const merge = verdicts.filter((v) => v.verdict === 'merge' && !v.emDashOrTrailer)
const fixFirst = verdicts.filter((v) => v.verdict === 'fix-first' || v.emDashOrTrailer)
const hold = verdicts.filter((v) => v.verdict === 'hold')
log('verify: ' + merge.length + ' merge, ' + fixFirst.length + ' fix-first, ' + hold.length + ' hold of ' + DEFAULT_PRS.length)

return {
  verdicts: verdicts.map((v) => ({ ref: v.ref, blast: v.blast, verdict: v.verdict, emDashOrTrailer: v.emDashOrTrailer, p0p1: (v.findings || []).filter((f) => f.sev === 'P0' || f.sev === 'P1'), summary: v.summary })),
  merge: merge.map((v) => v.ref),
  fixFirst: fixFirst.map((v) => ({ ref: v.ref, p0p1: (v.findings || []).filter((f) => f.sev === 'P0' || f.sev === 'P1'), emDashOrTrailer: v.emDashOrTrailer })),
  hold: hold.map((v) => ({ ref: v.ref, summary: v.summary })),
}
