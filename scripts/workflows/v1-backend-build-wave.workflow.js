export const meta = {
  name: 'v1-backend-build-wave',
  description: 'Build the v1 backend dependency spine (epic #734) generator-first: per trio root, scaffold via gen:service (right flag per existing core state) + author the domain TypeSpec contract + AsyncAPI events + REST resources from the issue spec, add replayable mocks + tests, ai/curaos mirror docs, commit the submodule(s) + open PR. Each lane runs in an isolated curaos worktree (gen:service touches the parent AppModule/barrel) so lanes do not collide. Generator-first, zero special edits.',
  phases: [
    { title: 'Build', detail: 'per trio root: assess + scaffold + domain contract + tests + submodule PR (parallel, worktree-isolated)' },
    { title: 'Verify', detail: 'adversarially verify a sample: contract compiles, REST resources present, no clobbered core' },
  ],
}

const TRACKER = 'your-org/curaos-ai-workspace'

const BUILT = { type: 'object', required: ['root', 'status'], properties: {
  root: { type: 'string' },
  status: { type: 'string', enum: ['done', 'partial', 'blocked', 'split'] },
  layersBuilt: { type: 'array', items: { type: 'string' } },
  prs: { type: 'array', items: { type: 'string' } },
  contractResources: { type: 'array', items: { type: 'string' } },
  events: { type: 'array', items: { type: 'string' } },
  generatorEvolution: { type: 'string' },
  foresight: { type: 'array', items: { type: 'string' } },
  evidence: { type: 'string' },
  blocker: { type: 'string' } } }

const VERDICT = { type: 'object', required: ['root', 'real', 'verdict'], properties: {
  root: { type: 'string' }, real: { type: 'boolean' }, verdict: { type: 'string' },
  issues: { type: 'array', items: { type: 'string' } } } }

const DEFAULT_UNITS = [{"root": "automation", "issues": [739, 741, 745], "slugs": ["automation-core-service", "business-automation-service", "personal-automation-service"], "specs": [{"n": 739, "title": "[v1][backend] Build automation-core-service (neutral, empty scaffold; spec-less)", "body": "CRITICAL PATH and HIGHEST FAN-OUT (empty scaffold, no spec; blocks 5 apps). Author TypeSpec FIRST, then fill the existing scaffold dir. Neutral Automation Core per charter: low-code actions + connectors + scheduling. REST resources consuming apps need: GET /connectors (connector catalogue/marketplace fetch - workflow-designer, business-automation, business-donation, business-workflow, personal-automation all fetch this), GET /connectors/{id}, POST /automations, GET /automations/{id}/runs (run history), connector property schema for node panels. Emit events: automation.run.started, automation.run.completed, connector.registered (versioned, outbox). Must satisfy: workflow-designer connector catalogue, business-automation connector marketplace, business-workflow node property panels, personal-automation connector fetch, business-donation automation wiring. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: workflow-designer, business-automation, business-donation, business-workflow, personal-automation\n\nParent: #734\n\nTarget Version: v1"}, {"n": 741, "title": "[v1][backend] Build business-automation-service (business overlay, empty scaffold; spec-less)", "body": "Empty scaffold, no spec. Author TypeSpec FIRST, then fill scaffold. Business variant over automation-core-service (depends on automation-core-service being built first). REST resources business-automation needs: automation editor CRUD POST/GET/PATCH /automations, approval-workflow gate POST /automations/{id}/approvals, environment-promotion POST /automations/{id}/promote, monitoring dashboard GET /automations/{id}/metrics. Emit events: automation.approved, automation.promoted. Must satisfy business-automation Done: create-approval-promote smoke path wires to real APIs. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: business-automation\n\nParent: #734\n\nTarget Version: v1"}, {"n": 745, "title": "[v1][backend] Build personal-automation-service (personal overlay, empty scaffold; spec-less)", "body": "Empty scaffold, no spec. Author TypeSpec FIRST, then fill scaffold. Personal variant over automation-core-service (depends on automation-core-service for connector catalogue). REST resources personal-automation needs: node-editor automation CRUD POST/GET/PATCH/DELETE /automations + save/load, run-history GET /automations/{id}/runs (polling), trigger dispatch POST /automations/{id}/trigger. Emit events: automation.created, automation.run.started. Must satisfy personal-automation Done: node-editor save/load + run-history web + Expo smoke. Reuse automation-core-service primitives; do not fork. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: personal-automation\n\nParent: #734\n\nTarget Version: v1"}]}, {"root": "calendar", "issues": [748], "slugs": ["personal-calendar-service"], "specs": [{"n": 748, "title": "[v1][backend] Build personal-calendar-service (personal overlay, empty scaffold; spec-less)", "body": "Empty scaffold, no spec. Author TypeSpec FIRST, then fill scaffold. Personal variant over calendar-core-service. REST resources personal-calendar needs: availability editor GET/PUT /availability, public booking page POST /bookings, booking management GET/PATCH/DELETE /bookings. Confirmations via notify-service. Emit events: booking.created, booking.cancelled. Must satisfy personal-calendar Done: availability + public booking + management web + Expo smoke. Reuse calendar-core-service primitives; do not fork. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: personal-calendar\n\nParent: #734\n\nTarget Version: v1"}]}, {"root": "donation", "issues": [742, 774], "slugs": ["business-donation-service", "personal-donation-service"], "specs": [{"n": 742, "title": "[v1][backend] Build business-donation-service (business overlay, empty scaffold; spec-less)", "body": "Empty scaffold, no spec. Author TypeSpec FIRST, then fill scaffold. Business variant over donation-core-service. REST resources business-donation needs: campaign CRUD POST/GET/PATCH/DELETE /campaigns, payout scheduling POST /payouts, compliance-report export GET /campaigns/{id}/report, campaign-stats GET /campaigns/{id}/stats (React Query polling), donor communication via notify-service. Compliance export + payout reconcile to accounting-core-service. Emit events: campaign.created, payout.scheduled, donation.received. Must satisfy business-donation Done: create campaign -> track progress -> export report smoke flow. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: business-donation\n\nParent: #734\n\nTarget Version: v1"}, {"n": 774, "title": "[v1][backend] Build personal-donation-service", "body": "Empty scaffold / no spec. Author TypeSpec FIRST, then fill via the service generator. Personal variant over donation-core-service. REST: donation/give flow, history, recurring schedule. Receipts via notify-service. Needed by: personal-donation. Generator-first; ai/curaos mirror docs + replayable mocks. Parent: #734. Target Version: v1."}]}, {"root": "healthstack-billing", "issues": [737], "slugs": ["healthstack-billing-service"], "specs": [{"n": 737, "title": "[v1][backend] Author TypeSpec + build healthstack-billing-service (HealthStack overlay)", "body": "CRITICAL PATH (no spec, no dir). Author TypeSpec FIRST, then scaffold under curaos/backend/services/healthstack-billing-service. HealthStack overlay (vertical -> neutral dep direction only; consider extending accounting-core-service for ledger primitives). PHI/PII lives in overlay schema. REST resources front-office needs: patient billing summary GET /patients/{id}/billing, charge/invoice list GET /invoices, copay/estimate at check-in POST /estimates, payment capture POST /payments. Emit events: invoice.issued, payment.captured (versioned, outbox). Must satisfy front-office Done: the billing screen wires to real API in the check-in -> consent -> queue flow. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: front-office\n\nParent: #734\n\nTarget Version: v1"}]}, {"root": "healthstack-consent", "issues": [778], "slugs": ["healthstack-consent-service"], "specs": [{"n": 778, "title": "[v1][backend] Build healthstack-consent-service", "body": "Empty scaffold / no spec. Author TypeSpec FIRST, then fill via the service generator. Empty scaffold (dir healthstack-consent-service; referenced as consent-core-service - name drift). HealthStack overlay consent capture + storage (PHI in overlay schema). REST: POST /consents (signature serialize), GET /consents, consent state. Events: consent.granted/revoked. Needed by: front-office. Generator-first; ai/curaos mirror docs + replayable mocks. Parent: #734. Target Version: v1."}]}, {"root": "healthstack-messaging", "issues": [738], "slugs": ["healthstack-messaging-service"], "specs": [{"n": 738, "title": "[v1][backend] Author TypeSpec + build healthstack-messaging-service (HealthStack overlay)", "body": "CRITICAL PATH (no spec, no dir). Author TypeSpec FIRST, then scaffold under curaos/backend/services/healthstack-messaging-service. HealthStack overlay for patient/provider secure messaging (PHI in overlay schema; dep direction vertical -> neutral, reuse notify-service for delivery transport). REST resources front-office needs: GET /threads, GET /threads/{id}/messages, POST /threads/{id}/messages, unread counts. Real-time delivery via notify-service gateway. Emit events: message.sent, thread.created. Must satisfy front-office Done: messaging surfaces wire to real API. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: front-office\n\nParent: #734\n\nTarget Version: v1"}]}, {"root": "notes", "issues": [775], "slugs": ["personal-notes-service"], "specs": [{"n": 775, "title": "[v1][backend] Build personal-notes-service", "body": "Empty scaffold / no spec. Author TypeSpec FIRST, then fill via the service generator. Personal notes: notes CRUD + folders + search + attachments. May extend storage-service. REST: GET/POST/PATCH/DELETE /notes, folders, full-text search. Needed by: personal-notes. Generator-first; ai/curaos mirror docs + replayable mocks. Parent: #734. Target Version: v1."}]}, {"root": "plugin-runtime", "issues": [736], "slugs": ["plugin-runtime-service"], "specs": [{"n": 736, "title": "[v1][backend] Author TypeSpec + build plugin-runtime (neutral, empty scaffold)", "body": "CRITICAL PATH (empty scaffold, no spec). Decide canonical kebab name (plugin-runtime-service) and author TypeSpec contract FIRST, then fill the scaffold. Neutral runtime that lists/installs/enables/disables platform plugins + extension-point registry per charter documented seams. REST resources admin-app needs: GET /plugins (catalogue), GET /plugins/{id}, POST /plugins/{id}/enable, POST /plugins/{id}/disable, GET /plugins/installed. Emit events: plugin.enabled, plugin.disabled. Must satisfy admin-app Done: the plugin page module renders against real REST. Define extension-point + config-hook data contracts (published seams). Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: admin-app\n\nParent: #734\n\nTarget Version: v1"}]}, {"root": "shop", "issues": [743, 776], "slugs": ["business-shop-service", "personal-shop-service"], "specs": [{"n": 743, "title": "[v1][backend] Build business-shop-service (business overlay, empty scaffold; spec-less)", "body": "Empty scaffold, no spec. Author TypeSpec FIRST, then fill scaffold. Business variant over commerce-core-service + inventory-core-service. REST resources business-shop needs: catalog CRUD POST/GET/PATCH/DELETE /products, order management GET/PATCH /orders, fulfillment state machine POST /orders/{id}/fulfill, inventory dashboard GET /inventory (via inventory-core-service), analytics from accounting-core-service. React Query mutations. Emit events: product.created, order.placed, order.fulfilled. Must satisfy business-shop Done: create product -> fulfill order smoke. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: business-shop\n\nParent: #734\n\nTarget Version: v1"}, {"n": 776, "title": "[v1][backend] Build personal-shop-service", "body": "Empty scaffold / no spec. Author TypeSpec FIRST, then fill via the service generator. Personal variant over commerce-core-service. REST: product mgmt, storefront, orders, payout, messaging. Needed by: personal-shop. Generator-first; ai/curaos mirror docs + replayable mocks. Parent: #734. Target Version: v1."}]}, {"root": "site", "issues": [740, 777], "slugs": ["business-site-service", "personal-site-service"], "specs": [{"n": 740, "title": "[v1][backend] Build business-site-service (business overlay, empty scaffold; spec-less)", "body": "HIGH FAN-OUT (empty scaffold, no spec; blocks 3 apps). Author TypeSpec FIRST, then fill scaffold. Business variant extending site-core-service per [[curaos-triplet-split-rule]] (named divergent business subject ownership). REST resources consuming apps need: site CRUD GET/POST/PATCH/DELETE /sites, publish flow POST /sites/{id}/publish, canvas document save/load GET/PUT /sites/{id}/document, commerce block embedding metadata. WS presence via notify-service gateway. Emit events: site.published, site.updated. Must satisfy: business-site (CRUD + publish + canvas save/load + commerce embed), business-shop (storefront embedding), business-donation (campaign site). Reuse site-core-service primitives; do not fork. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: business-site, business-shop, business-donation\n\nParent: #734\n\nTarget Version: v1"}, {"n": 777, "title": "[v1][backend] Build personal-site-service", "body": "Empty scaffold / no spec. Author TypeSpec FIRST, then fill via the service generator. Personal variant over site-core-service. REST: site CRUD + publish + canvas document save/load + template gallery. Needed by: personal-site. Generator-first; ai/curaos mirror docs + replayable mocks. Parent: #734. Target Version: v1."}]}, {"root": "tenancy", "issues": [735], "slugs": ["tenancy-core-service"], "specs": [{"n": 735, "title": "[v1][backend] Author TypeSpec + build tenancy-core-service (neutral)", "body": "CRITICAL PATH (no spec, no dir). Author TypeSpec contract FIRST under contracts, then scaffold the NestJS service under curaos/backend/services/tenancy-core-service. Neutral capability owning tenant lifecycle + per-tenant isolation metadata (NOT PHI/PII). REST resources the consuming app needs: POST/GET/PATCH/DELETE /tenants (tenant CRUD), GET /tenants/{id}/settings, tenant activation/suspension state machine, plan/quota metadata. Emit durable events: tenant.created, tenant.updated, tenant.suspended, tenant.deleted (versioned, outbox pattern). Must satisfy admin-app Done: tenant CRUD pages + Playwright tenant-CRUD E2E need live REST. DB-per-tenant references + metadata only per charter (neutral services carry references + metadata, not tenant PHI). Wire RBAC via identity-service. Follow [[curaos-triplet-split-rule]] (neutral only, no triplet). Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: admin-app\n\nParent: #734\n\nTarget Version: v1"}]}, {"root": "tracking", "issues": [746], "slugs": ["personal-tracking-service"], "specs": [{"n": 746, "title": "[v1][backend] Build personal-tracking-service (personal overlay, empty scaffold; spec-less)", "body": "Empty scaffold, no spec; current scaffold implements the WRONG domain (habit tracker) and must be REBUILT to its real domain per ADR-0219: personal location-sharing + geofence + SOS. Author TypeSpec FIRST, then fill scaffold. Personal overlay (consent-gated; PII in overlay schema; reuse geospatial-core-service primitives). REST resources fleet-manager needs: live position feed GET /tracking/{subjectId}/positions + WS stream, geofence CRUD, SOS alert state. Consent gate before any personal-tracking feed is rendered. Emit events: tracking.position.updated, geofence.entered, sos.raised. Must satisfy fleet-manager Done: personal-tracking feeds path in the dispatch board. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: fleet-manager\n\nParent: #734\n\nTarget Version: v1"}]}, {"root": "workflow", "issues": [744, 747], "slugs": ["business-workflow-service", "personal-workflow-service"], "specs": [{"n": 744, "title": "[v1][backend] Build business-workflow-service (business overlay, empty scaffold; spec-less)", "body": "Empty scaffold, no spec. Author TypeSpec FIRST, then fill scaffold. Business variant over workflow-core-service (depends on automation-core-service for connector catalogue). REST resources business-workflow needs: process-definition CRUD POST/GET/PATCH/DELETE /process-definitions + versioning, deployment-promotion pipeline POST /process-definitions/{id}/promote, approval gate POST /process-definitions/{id}/approvals, change-request flow. Emit events: process.definition.published, process.definition.promoted. Must satisfy business-workflow Done: edit-flow -> change-request -> promote smoke path wires to real APIs. Reuse workflow-core-service primitives; do not fork. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: business-workflow\n\nParent: #734\n\nTarget Version: v1"}, {"n": 747, "title": "[v1][backend] Build personal-workflow-service (personal overlay, empty scaffold; spec-less)", "body": "Empty scaffold, no spec. Author TypeSpec FIRST, then fill scaffold. Personal variant over workflow-core-service. REST resources personal-automation needs: workflow-action trigger dispatch POST /workflows/{id}/trigger, personal workflow CRUD GET/POST/PATCH /workflows. Emit events: workflow.triggered. Must satisfy personal-automation Done: workflow-action trigger dispatch path. Reuse workflow-core-service primitives; do not fork. Add Requirements.md + AGENTS.md + CONTEXT.md under ai/curaos mirror + replayable mocks.\n\nNeeded by: personal-automation\n\nParent: #734\n\nTarget Version: v1"}]}]

const PROMPT_HEAD =
  'Build a CuraOS backend trio to v1 contract-level functional parity, GENERATOR-FIRST, in your ISOLATED worktree. You are the sole owner of this lane; other lanes own every other service.\n\n' +
  'WORKFLOW (do ALL, generator-first per ADR-0219 + curaos_generator_evolution_rule, zero special edits):\n' +
  '1. You are in an isolated git worktree of the workspace; the curaos checkout is at <worktree>/curaos (HEAD=main, with the #739 gen:service fix). Confirm with: cd <worktree>/curaos then git rev-parse --abbrev-ref HEAD and (rg -c requiredLiteralDirs tools/codegen/src/service-lock-emit.ts) must print 2 (the fix is present).\n' +
  '2. ASSESS existing state per layer slug: ls backend/services/<slug>/src | wc -l and ls backend/services/<slug>/specs. A populated <root>-core-service (many src files) MUST NOT be clobbered - reuse it.\n' +
  '3. INIT the target submodule(s): git submodule update --init backend/services/<slug> for each slug, AND the <root>-core-service if an overlay extends it (the lock needs the core package.json present, else the #739 guard fails closed). For the workflow trio also init backend/services/identity-service (its packages/auth-sdk is a nested lock dep).\n' +
  '4. SCAFFOLD with the RIGHT flag, NEVER clobbering a populated core:\n' +
  '   - whole trio empty: bun run gen:service <root> --write\n' +
  '   - overlay only, core exists populated: bun run gen:service <root> --personal-only --write and/or --business-only --write per the slugs; the existing core stays untouched.\n' +
  '   - HealthStack: bun run gen:service <root> --overlay healthstack --write (PHI in overlay schema).\n' +
  '   - neutral new core (tenancy/plugin-runtime): bun run gen:service <root> --core-only --write (emits a LOOSE dir in the curaos parent if no submodule repo exists - acceptable; commit it in the curaos parent on your branch).\n' +
  '   - notes/shop (no <root>-core): decide plain vs core+overlay from the spec; prefer extending an existing neutral core (commerce-core for shop, storage-service for notes) where the spec says so.\n' +
  '   Exit 0 + a bun.lock=created line proves scaffold+lock worked. If the lock 404s on a nested @curaos/* dep, init that dep submodule and re-run; if the generator still cannot resolve it, that is a GENERATOR gap - record it in generatorEvolution + foresight, do NOT hand-edit the lock.\n' +
  '5. AUTHOR THE DOMAIN CONTRACT: edit backend/services/<slug>/specs/<name>.tsp to add EXACTLY the REST resources the spec lists (operations, typed request/response models with status codes, @route namespaces) and <name>.asyncapi.yaml for the events. Keep the controller (src/.../*.controller.ts) in lock-step with the .tsp, backed by the drizzle repo + a replayable mock for the contract-mock path. CONTRACT-MOCK level (no live infra): the route shapes + DTOs + a mock fixture are the deliverable; persistence is welcome but the bar is a real contract the SDK can generate from.\n' +
  '6. MIRROR DOCS: write/refresh ai/curaos/backend/services/<slug>/Requirements.md + AGENTS.md + CONTEXT.md (fill the domain). NEVER put service docs inside the code submodule (repo-boundary rule).\n' +
  '7. GENERATOR-FIRST: any recurring gap (a contract pattern, a missing emitter capability, a lock edge) gets fixed in tools/codegen + recorded in generatorEvolution - NOT a per-service hand edit. A genuinely singular edit is allowed but must be justified.\n' +
  '8. VERIFY: cd backend/services/<slug> then tsp compile specs/<name>.tsp (contract compiles to OpenAPI, exit 0) + bun test if tests exist. Paste the last ~12 lines + exit code into evidence.\n' +
  '9. COMMIT + PR per submodule (or the curaos parent for loose-dir cores): branch feat/v1-backend-<root>, conventional commit (NO AI-attribution trailers, NO em/en dashes), push, gh pr create with Closes #<issue> for each issue satisfied. Own submodule repo -> PR there; loose dir in curaos parent -> PR to curaos. Use env -u GITHUB_TOKEN gh.\n' +
  '10. NEVER push to main. NEVER --no-verify around a real failure. If blocked on something genuinely external, report status=blocked with the exact blocker.\n\n' +
  'Report: root, status (done|partial|blocked|split), layersBuilt, prs, contractResources (REST routes authored), events, generatorEvolution (what you folded into tools/codegen or "none"), foresight, evidence (verify tails + exit), blocker. Repo-boundary: your trio submodules + curaos parent for loose cores + ai/curaos mirror only.\n\n'

function unitPrompt(u) {
  const specs = u.specs.map((s) => '### #' + s.n + ' ' + s.title + '\n' + s.body).join('\n\n')
  return PROMPT_HEAD +
    'THIS LANE - trio root "' + u.root + '", satisfies [v1][backend] issues ' + JSON.stringify(u.issues) +
    ' in repo ' + TRACKER + ', service slugs ' + JSON.stringify(u.slugs) + ', all parented to #734, Target Version v1.\n\n' +
    'Domain specs (the exact REST resources + events each consuming app needs):\n' + specs
}

const UNITS = (typeof args !== 'undefined' && args && Array.isArray(args.units)) ? args.units : DEFAULT_UNITS

phase('Build')
const built = await parallel(UNITS.map((u) => () =>
  agent(unitPrompt(u), { label: 'build:' + u.root, phase: 'Build', model: 'opus', isolation: 'worktree', schema: BUILT })
    .then((r) => ({ ...r, unit: u }))
    .catch((e) => ({ root: u.root, status: 'blocked', blocker: String((e && e.message) || e), prs: [] }))
)).then((r) => r.filter(Boolean))

const done = built.filter((b) => b.status === 'done')
const partial = built.filter((b) => b.status === 'partial')
const blocked = built.filter((b) => b.status === 'blocked')
log('backend build: ' + done.length + ' done, ' + partial.length + ' partial, ' + blocked.length + ' blocked of ' + UNITS.length)

phase('Verify')
const sample = [...done, ...partial].slice(0, 6)
const verdicts = await parallel(sample.map((b) => () =>
  agent(
    'Adversarially verify the CuraOS backend trio "' + b.root + '" was genuinely built to v1 contract-level parity. The lane reported: status=' + b.status +
    ', layers=' + JSON.stringify(b.layersBuilt || []) + ', PRs=' + JSON.stringify(b.prs || []) + ', contractResources=' + JSON.stringify(b.contractResources || []) + '.\n\n' +
    'Check on the PR branch / merged state via env -u GITHUB_TOKEN gh and the repo:\n' +
    '1. The .tsp contract for each built layer actually declares the REST resources the issue spec required (not just the scaffold stub). Try to find a CLAIMED route NOT in the .tsp.\n' +
    '2. tsp compile specs/<name>.tsp succeeds.\n' +
    '3. A populated <root>-core-service was NOT clobbered (its prior domain code intact).\n' +
    '4. The AsyncAPI events the spec required are present.\n' +
    '5. ai/curaos mirror docs exist; no service docs leaked into the code submodule.\n' +
    '6. No em/en dashes; no AI-attribution commit trailers.\n' +
    'Default real=false if a claimed route/event is missing, the compile fails, a core was clobbered, or docs leaked. Report root="' + b.root + '", real, verdict, issues.',
    { label: 'verify:' + b.root, phase: 'Verify', model: 'opus', schema: VERDICT }
  ).catch(() => null)
)).then((r) => r.filter(Boolean))

return {
  built: built.map((b) => ({ root: b.root, status: b.status, layersBuilt: b.layersBuilt, prs: b.prs, contractResources: b.contractResources, events: b.events, generatorEvolution: b.generatorEvolution, foresight: b.foresight, blocker: b.blocker })),
  done: done.map((b) => b.root),
  partial: partial.map((b) => b.root),
  blocked: blocked.map((b) => ({ root: b.root, blocker: b.blocker })),
  verdicts: verdicts.map((v) => ({ root: v.root, real: v.real, issues: v.issues })),
  verifyFails: verdicts.filter((v) => !v.real).map((v) => ({ root: v.root, issues: v.issues })),
  allPRs: built.flatMap((b) => b.prs || []),
  allForesight: built.flatMap((b) => b.foresight || []).filter(Boolean),
  generatorEvolutions: built.map((b) => ({ root: b.root, ge: b.generatorEvolution })).filter((x) => x.ge && x.ge !== 'none'),
}
